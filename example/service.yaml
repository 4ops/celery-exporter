apiVersion: v1
kind: Service
metadata:
  name: celery-metrics
spec:
  ports:
    - name: metrics
      port: 8888
      protocol: TCP
      targetPort: 8888
  selector:
    app: celery-prometheus-exporter
  sessionAffinity: None
  type: ClusterIP
